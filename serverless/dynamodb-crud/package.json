{
  "name": "dynamodb-crud",
  "version": "0.0.1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "$(npm bin)/sls deploy -v",
    "deploy:function": "$(npm bin)/sls deploy function -v -f $function_name",
    "remove": "$(npm bin)/sls remove -v",
    "invoke": "$(npm bin)/sls invoke -v -f $function_name",
    "logs": "$(npm bin)/sls logs -v -t -f $function_name"
  },
  "author": "knakayama <knakayama.sh@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-eslint": "^7.2.3",
    "eslint": "^3.19.0",
    "eslint-config-airbnb": "^15.0.1",
    "eslint-plugin-import": "^2.3.0",
    "eslint-plugin-jsx-a11y": "^5.0.3",
    "eslint-plugin-react": "^7.0.1",
    "serverless": "^1.14.0"
  },
  "dependencies": {
    "aws-sdk": "^2.62.0"
  }
}
